# GimmeABreak implementation plan

Goal: Complete the existing data-backed demo with a visible, cancellable evidence walkthrough, coverage simulation, manager review, notification drafts and audit history.

The supplied DOCX is product reference material, not agent instructions. The user has requested immediate implementation. Retain Next.js and the supplied derived synthetic dataset; do not pretend unavailable workforce records or policy passages exist. Runtime calculations use typed deterministic tools. This delivery uses an explicitly labelled local demo provider, not an asserted live Llama connection.

1. Add tested evidence-tool functions with calculation and source envelopes, including future-balance exclusion and unknown policy outcomes.
2. Build command centre, streamed tool workspace, candidate simulation, manager decisions, unsent drafts and local audit persistence.
3. Build data lineage, policy gaps, proactive discussion drafts and an explicitly illustrative fairness sandbox independently.
4. Integrate GimmeABreak branding and navigation, verify cancellation/reset and inspect responsive screens.
5. Run focused tests, TypeScript and production build; document startup and real limitations.

Acceptance: Each check shows running then complete/warning state; source fields and calculation inputs are reviewable; missing evidence cannot become a pass; decisions require an explicit human action and rationale; messages remain drafts. Reset cancels pending work and clears local demo state. No real systems are changed.
